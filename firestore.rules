rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All access goes through the Next.js server — no browser SDK access.
    // Auth is handled via HMAC session cookies at the server layer.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
